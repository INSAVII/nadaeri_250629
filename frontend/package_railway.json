{
  "scripts": {
    "build": "NODE_ENV=production parcel build src/index.html --dist-dir dist --no-source-maps --no-optimize --no-scope-hoist",
    "start": "serve dist -s -p $PORT",
    "dev": "parcel src/index.html --dist-dir dist"
  },
  "dependencies": {
    "@types/node": "^20.14.8",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.23.1",
    "typescript": "^5.4.5",
    "parcel": "^2.12.0",
    "serve": "^14.2.3"
  },
  "devDependencies": {
    "postcss": "^8.4.38",
    "process": "^0.11.10"
  },
  "targets": {
    "default": {
      "distDir": "dist"
    }
  }
}
